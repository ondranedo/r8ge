#ifndef R8GE_RECTANGLE_H
#define R8GE_RECTANGLE_H

#include "Mesh.h"
#include "Types.h"

namespace r8ge {
   /* class Rectangle : protected Mesh2D {
    public:
        ~Rectangle() override = default;
        Rectangle(coord_float width, coord_float height);

        using Mesh2D::getIndices;
        using Mesh2D::getVertices;
    };*/
}

#endif//!R8GE_RECTANGLE_H
