#include "Rectangle.h"

namespace r8ge {
  /*  Rectangle::Rectangle(coord_float width, coord_float height) {
        MeshTrinagle t1 = {{
                                   Vertex2D(0.0f, 0.0f),
                                   Vertex2D(width, 0.0f),
                                   Vertex2D(0.0f, height)
                           }};
        MeshTrinagle t2 = {{
                                   Vertex2D(width, 0.0f),
                                   Vertex2D(width, height),
                                   Vertex2D(0.0f, height)
                           }};

        addTriangle(t1);
        addTriangle(t2);
    }*/
}